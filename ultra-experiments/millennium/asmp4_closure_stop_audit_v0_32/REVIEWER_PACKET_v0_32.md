# Reviewer packet v0.32

## Decision under review

Stop autonomous bounded finite/local ASMP-4 expansion, while retaining the
classification `unresolved` at full frozen scope.

## Falsification path

The decision should be rejected if a reviewer can establish any of these:

1. one of the seven claimed conditional layers is not actually closed;
2. the canonical source already defines a unique formal global class with the
   needed architecture and quotient semantics;
3. finite exact public quotients are necessary despite the sealed Thue-Morse
   counterboundary;
4. another bounded finite fixture can logically prove abstraction existence
   for the unspecified nonlinear class; or
5. the requirement ledger marks a full-scope obligation missing even though a
   cited theorem proves it at that scope.

## Fast audit

1. Compare the five parsed rows with canonical lines 483-493.
2. Inspect the v0.15 semantic qualification and pending adjudication queue.
3. Inspect v0.26's finite-quotient nonnecessity boundary.
4. Verify that v0.31 closes the finite adversarial additive endpoint.
5. Check the dependency graph and all four reopening conditions in
   `STOP_CERTIFICATE_v0_32.md`.
6. Run both implementations and the focused tests.

## Full resolution guard

No passing gate in this package changes `full_scope_proved` above zero. A green
audit proves only that the stopping argument is internally supported by the
sealed evidence; it does not prove the original nonlinear conjecture.
