# Result v0.36

The final ASMP-4 audit supports a single-root operational stop on further
autonomous work.

Two independent parsers recover all five canonical completion requirements
and six explicit architecture clauses.  They also confirm seven missing domain
selectors: sensor grammar, randomness/disturbance order, normally-hyperbolic
and local-controllability predicates, public belief semantics, reset/closing
semantics, and actuator-dictionary domain.

The absence is material.  The sealed v0.6 fork gives regions
`[1,infinity) x [1,infinity)` and `[2,infinity) x [1,infinity)` for two sensor
registries that both satisfy the explicit source obligations.  The sealed
v0.8 fork separates per-disturbance from uniform almost-sure safety while the
source selects neither probability order.

The v0.33-v0.35 chain has pursued the remaining nonfinite theorem route:
componentwise full-region equality, compact public safe closing, robust
fixed-reset atlases, coordinate transport, and exact finite corrections now
exist under explicit hypotheses.  Every canonical requirement has conditional
evidence, but frozen-scope completion remains `0/5` because no normative domain
is selected.

Central and import-independent implementations verify six sealed resources,
the five requirements, six present architecture clauses, seven missing
selectors, both material forks, the single-root dependency graph, all 32
stopping assignments, and eight hostile mutations.  The predecessor inventory
is frozen at 36 packages and 394 tests.

V0.36 adds ten focused tests. The explicit 37-package chain passes all 404
tests in 257.31 seconds with Python bytecode and pytest caching disabled.

The decision is to stop autonomous ASMP-4 work until a formal registry,
attributable semantic adjudication, a sealed error, or a registry-independence
theorem appears.  This is not a resolution of ASMP-4 and not a proof that every
formal completion is impossible.
