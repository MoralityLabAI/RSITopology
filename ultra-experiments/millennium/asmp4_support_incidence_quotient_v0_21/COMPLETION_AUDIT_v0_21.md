# Completion audit v0.21

## Proved

- The active signature quotient is the coarsest exact-support-preserving
  quotient up to duplicate labels.
- Feasibility, renaming invariance, clone invariance, and idempotence hold.
- The quotient observer supplies exact semantic finite and asymptotic rates.
- Independent row and column enumerations reproduce the 53,108-relation
  census and identify all 260 duplicate-bearing feasible relations.
- The 104 deterministic embeddings are unchanged.
- Five quotient-definition mutations are rejected.

## Verification boundary

The predecessor inventory contains 21 packages and 244 tests. This package
adds 10 focused tests, so the expanded chain contains 254 tests. The explicit
22-package regression passed all 254 tests in 293.88 seconds.

## Not claimed

The exact-support quotient is not proved minimal for safety control. The
package does not select global source semantics, handle continuous observation
spaces, or establish the full nonlinear ASMP-4 variational theorem.
