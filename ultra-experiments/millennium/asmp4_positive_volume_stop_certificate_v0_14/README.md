# ASMP-4 positive-volume stop certificate v0.14

**Successor qualification:** the
[v0.15 semantic audit](../asmp4_semantic_selector_audit_v0_15/RESULT.md) rejects
the inference from six absent phrases to proved semantic underdetermination.
The local-fixture stop remains an operational recommendation, but the source
may intentionally parameterize registered sensor experiments. The frozen
v0.14 JSON claim is preserved for auditability.

This package turns the repaired v0.13 collar theorem into a harness-backed stopping decision. The earlier registry fork now survives bounded interval authority, arbitrary registered mode disturbances, a compact connected one-dimensional NHIM, positive initial volume, and exact finite-horizon safety-margin corrections.

The remaining fork is normative: computed and forced-raw sensor completions of the same physical data have different exact regions, while the canonical ASMP-4 text does not select a sensor observation/computation registry. More bounded local constructions cannot make that textual choice.

Read [HARNESS_STOP_CERTIFICATE_v0_14.md](HARNESS_STOP_CERTIFICATE_v0_14.md) for the argument and reopening conditions.

Run:

```powershell
python run_verification.py
python verify_positive_volume_stop.py
python -m pytest -q test_positive_volume_stop.py
```

This is a stop certificate for local harness work, not a claim that ASMP-4 is mathematically resolved.
