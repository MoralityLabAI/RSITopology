# Reviewer packet v0.14

**Successor notice:** v0.15 finds eight clause-level blockers to the strong
semantic no-selector claim. Review v0.14 as a frozen intermediate claim and
use the [v0.15 packet](../asmp4_semantic_selector_audit_v0_15/REVIEWER_PACKET_v0_15.md)
for the current disposition.

## Claim to review

The current repository-local ASMP-4 construction program should stop. The reason is not test exhaustion alone: two exact nondegenerate completions remain incompatible, and choosing between them requires a sensor-registry clause absent from the frozen source.

## How to falsify the stop

Any one of these would defeat or reopen it:

- identify an existing canonical clause that uniquely fixes the sensor observation/computation registry;
- find a proof or implementation error in the v0.13 exact regions or finite-margin converse;
- specify a source-mandated perturbation/noise class and prove that it collapses the two regions;
- supply an attributable theorem or expert mapping from the current wording to one unique formal class.

## Fast reproduction

Run the central and import-independent verifiers, then the focused tests listed in `README.md`. Inspect the frozen v0.14 JSON claim and verify the three SHA-256 seals.

## Important distinction

The certificate stops local bounded construction. It does not prove the broad variational program impossible and does not call ASMP-4 resolved. A parameterized theorem over explicitly registered sensor experiments remains mathematically meaningful.
