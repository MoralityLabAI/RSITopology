# ASMP-3 strict-FIX disposition v2.19

## Safe label

Use:

```text
V0.1 source order supports strict FIX as the conservative natural reading.
Under strict FIX, the displayed iff is internally refuted exactly in both
directions. The unrestricted classification and mathematical impossibility of
ASMP-3 are not established.
```

Do not use:

```text
ASMP-3 is impossible.
ASMP-3 is fully solved.
ADM is proved clause-preserving.
The source formally entails FIX.
```

## Evidence map

| Question | Evidence | Status |
|---|---|---|
| Is `FIX` the conservative reading? | canonical game is frozen before protocol admission | supported interpretive judgment |
| Is `FIX` formally entailed? | v0.1 has no formal English semantics or typed scope rule | not proved |
| Is `ADM` a useful successor target? | typed v0.2 defines `E`, `G`, and `Interfaces(E)` | yes, nonnormative |
| Is `ADM` clause-preserving for v0.1? | requires added objects and shifted freeze scope | not established |
| Does v0.7 refute FIX sufficiency? | exact gap `(3/5)^d -> 0` with all three conditions | yes, internally |
| Does v2.5 refute literal necessity? | unchanged protocol with nonbinding `Refute` variants | yes, internally |
| Does that resolve the broader classification? | canonical two-sided rule and five complete-resolution items | no |
| Is ASMP-3 impossible? | no no-characterization or full-class impossibility theorem | not proved |

## Remaining mathematical target

A legitimate continuation must first register the domain of the theorem.  The
most conservative choice is a strict fixed-interface class with explicit
reductions and resource invariance.  Once frozen, it can support a genuine
replacement characterization or a genuine no-characterization theorem.
