# ASMP-9 v0.6 execution-label note

`THEOREM_DRAFT_v0_6.md` retains its preregistration status line because that
file was hash-sealed before execution and was not rewritten after outcomes
were observed. The prospective registration is
`registration_v0_6.json`; the executed result is in `artifacts_v0_6/`; and the
independent post-run record is `verification_v0_6.json`.

The passing run verifies the registered implementation and finite access
consequences. It does not convert the classical theorem into a novelty claim
or a complete ASMP-9 resolution.
