# ASMP-9 sharp query-width theorem v0.3.1

Version 0.3.1 repairs the registered v0.3 endpoint error. The sharp robust
width formula applies on:

```text
0 < delta < 1,
```

not at `delta=0`, where exact ties provide additional information.

The theorem, proof, endpoint control, fresh verification grid, and immutable
v0.3 failure record are all retained. This is a finite cycle-coordinate
theorem and not a full ASMP-9 resolution.

