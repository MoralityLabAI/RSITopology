# ASMP-9 v0.41 sequential risk-access confirmation protocol

## Status

`prospective_unregistered`

The deterministic four-target fixture in `DEVELOPMENT_PROTOCOL_v0_41.json`
and its `1/2 -> 1/4` correction are burned. They cannot support this
confirmation.

## Primary object

For registered finite decision problem `d`, query library `Q`, and horizon
`h`, the primary object is:

```text
U_h(d,Q) = conv(V_h(d,Q)) + R_+^Theta,
```

where `V_h` is generated by the frozen adaptive Bellman risk recursion in
`THEOREM_DRAFT_v0_41.md`.

The directed access deficiency relative to perfect revelation is the smallest
`epsilon` such that the zero reference risk lies in:

```text
U_h(d,Q) + epsilon*1.
```

## Disjoint rational-channel fixture

Four targets use three binary channels. Their noiseless signatures are:

```text
root_q  = (0,0,1,1)
left_q  = (0,1,0,0)
right_q = (0,0,0,1).
```

Each response is independently flipped conditional on the target:

```text
root_q error  = 1/5
left_q error  = 1/4
right_q error = 1/3.
```

The primary decision problem is four-target zero-one identification. A
decision-type negative control uses root-group labels `(0,0,1,1)`,
false-positive cost `1/3`, and false-negative cost `2/3`.

## Frozen exact predictions

Classification deficiency:

| Horizon | Adaptive | Nonadaptive open-loop |
| ---: | ---: | ---: |
| 0 | `3/4` | `3/4` |
| 1 | `3/5` | `3/5` |
| 2 | `61/135` | `13/25` |

The horizon-two adaptivity gain is:

```text
13/25 - 61/135 = 46/675.
```

For the asymmetric root-group decision at horizon two:

```text
adaptive = nonadaptive = 4/45.
```

This negative control requires that adaptivity be valuable for the branching
four-way decision but not automatically for every loss type on the same
channels.

## Gates

- `P0`: all fourteen preregistration tests pass.
- `S0`: every sealed source, protocol, fixture, and environment hash matches.
- `R0`: exactly six classification cells and two group cells are emitted.
- `B0`: the adaptive classification curve equals `3/4, 3/5, 61/135`.
- `N0`: adaptive and nonadaptive values agree at horizons zero and one.
- `A0`: horizon-two values are `61/135` and `13/25`, giving gap `46/675`.
- `D0`: both root-group modes equal `4/45`.
- `X0`: each exact minimax certificate equalizes target risks.
- `RESOURCE`: execution remains below 600 seconds, 1 GiB, one process, and
  zero network.

Every gate is binding. Any failure returns `registered_gate_failed`; no
threshold or fixture may be revised after registration.

## Claim boundary

A pass establishes the exact finite Bellman risk recursion and a strict
adaptive-versus-open-loop separation in this registered rational fixture.
It does not establish a new sequential-design theorem, an efficient
policy-search algorithm, finite-sample validity, unknown-channel robustness,
strategic-demonstrator safety, a real-model result, or ASMP-9 resolution.
