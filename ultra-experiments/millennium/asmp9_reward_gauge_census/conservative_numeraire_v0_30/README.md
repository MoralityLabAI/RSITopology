# ASMP-9 conservative numeraire v0.30

Development-only successor to calibrated occupancy access v0.28 and the
decision-relevance bridge v0.29.

The candidate theorem separates:

```text
mechanical noninterference,
semantic additive calibration,
and unknown-coefficient scale ambiguity.
```

Run the burned development tests:

```powershell
python -m pytest `
  ultra-experiments/millennium/asmp9_reward_gauge_census/conservative_numeraire_v0_30 `
  -q
```

No registration or claim-eligible result exists yet.
