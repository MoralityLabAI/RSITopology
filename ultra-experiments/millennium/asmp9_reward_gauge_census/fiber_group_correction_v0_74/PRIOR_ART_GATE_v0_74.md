# Prior-art gate for ASMP-9 fiber-group correction v0.74

Status: **classical group-action and set-cover specialization**.

## Subsumption

No mathematical novelty is claimed for:

- the full symmetric group on fibers of a set map;
- equality of an orbit partition and an observational-equivalence partition;
- pair separation by a family of tests; or
- the reduction of minimum separating access to set cover.

These are elementary finite group action and combinatorial optimization facts.

## Reward-learning boundary

Potential-based shaping and modern reward-learning invariance results concern
specific transformations that preserve a declared behavior or decision
criterion. Version v0.74 does not enlarge that literature. It prevents a
different logical mistake:

```text
an arbitrary permutation preserving observations
is not thereby
a licensed reward transformation.
```

The observation map determines fibers. The decision problem must license the
gauge independently. Exact identification is agreement of the two partitions.

## Relation to v0.69

The v0.69 joint linear quotient theorem is the linear-algebraic instance:

```text
observation-equivalent displacement space = A^-1(N+A(G));
licensed displacement space               = G.
```

Equality is exact quotient identification. Version v0.74 shows why this
partition comparison, not discovery of a maximal abstract symmetry group, is
the canonical formulation.

## Residual contribution

The ASMP-9-specific contribution is a correction to the problem statement and
an executable three-way diagnostic:

- `overdiscriminates_licensed_gauge`;
- `underidentified`; or
- `exact_quotient_identification`.

Before circulation, primary citations should be pinned for potential-based
reward shaping, reward-learning invariance, minimum test covers, and set cover.

## Hostile-review questions

1. Who licensed the declared gauge, and against which decision family?
2. Does a query split transformations claimed to be behaviorally irrelevant?
3. Are observation-law equalities exact or estimated from finite data?
4. Does the query registry omit interventions that would refine the fibers?
5. Is the full fiber-permutation group being misreported as a physical
   symmetry?

All remain outside the finite development claim.
