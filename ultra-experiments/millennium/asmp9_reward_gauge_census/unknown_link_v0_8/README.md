# ASMP-9 unknown response-link frontier v0.8

Development seed for the response-parameter gap identified by the v0.7
resolution audit.

The candidate theorem separates:

1. a known link with unknown positive inverse temperature, which adds only the
   already-declared positive-scale gauge; and
2. an unrestricted unknown monotone symmetric link on a finite comparison
   design, which admits non-affine utility ambiguity even under exact complete
   pairwise probabilities.

Run:

```powershell
python -m pytest `
  ultra-experiments/millennium/asmp9_reward_gauge_census/unknown_link_v0_8/test_unknown_link.py `
  -q
```

This directory is unregistered development work. No claim-eligible v0.8 run
has occurred.
