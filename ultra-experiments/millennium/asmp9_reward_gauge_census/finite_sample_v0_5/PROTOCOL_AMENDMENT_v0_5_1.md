# ASMP-9 finite-sample protocol amendment v0.5.1

## Amendment

This is an execution-only amendment to v0.5 after a pre-outcome repository-root
failure.

- replace protocol identifier `ASMP-9-FINITE-SAMPLE-v0.5` with
  `ASMP-9-FINITE-SAMPLE-v0.5.1`;
- set the runner and verifier repository root to `HERE.parents[3]`;
- retain every scientific cell, seed, gate, tolerance, resource cap, theorem,
  and success verdict from v0.5.

The v0.5 failure is preserved in `RUNNER_PATH_FAILURE_v0_5.md`. No outcome was
available when this amendment was written.
