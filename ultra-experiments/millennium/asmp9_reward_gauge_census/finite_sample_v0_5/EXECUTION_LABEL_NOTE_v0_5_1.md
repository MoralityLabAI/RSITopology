# v0.5.1 artifact-label note

The v0.5.1 path-correction wrapper deliberately invokes the sealed v0.5 runner.
Consequently the generated filenames and Markdown title retain `v0.5`, while
the machine-readable protocol identifier in both result and receipt is:

```text
ASMP-9-FINITE-SAMPLE-v0.5.1
```

The protocol identifier is authoritative. This is a presentation inheritance,
not a scientific or provenance ambiguity.
