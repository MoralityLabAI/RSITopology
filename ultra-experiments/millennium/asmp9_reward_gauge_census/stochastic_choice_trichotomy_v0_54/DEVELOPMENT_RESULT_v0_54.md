# ASMP-9 stochastic-choice trichotomy development result v0.54

## Status

**`development_only_not_claim_eligible`**

This run was used to test the grammar and set prospective verification
targets. It is not the sealed result.

## Census

The complete positive denominator-six grid on three alternatives contains
`1,250` stochastic choice kernels:

| classification | kernels |
|---|---:|
| `scalar_luce` | 1 |
| `random_utility_non_luce` | 230 |
| `no_random_utility_representation` | 1,019 |

The exact Block-Marschak and ranking-simplex classifiers disagreed on zero
kernels. Every rationalizable kernel carried a nonnegative exact ranking
mixture, and every non-rationalizable kernel carried a negative
Block-Marschak polynomial.

## Access witness

Two complete kernels have identical probabilities on every binary menu:

```text
p(a | ab) = p(a | ac) = p(b | bc) = 1/2.
```

The uniform ternary completion is Luce-scalar. The completion
`(3/4, 1/8, 1/8)` has no random-utility representation because it violates
regularity. Thus binary-menu access cannot decide even the endpoints of the
registered trichotomy.

## Interpretation

The result identifies an executable answer to one ASMP-9 well-posedness
question:

```text
one scalar ratio scale
    versus
a weaker stochastic preference object
    versus
no object in the declared random-utility class.
```

The representation mathematics is classical Luce/Falmagne/McFadden-Richter.
The prospective contribution is the exact, certificate-bearing ASMP-9
instrument and its access ledger.

## Claim boundary

This development run is not a proof, a finite-sample test, evidence about a
human or model, a welfare claim, general incomplete-menu identification, or an
ASMP-9 resolution.

