# ASMP-9 v0.28 development note

## Burned fixtures

The development tests used:

- three- and four-coordinate path-difference matrices;
- one fixed six-step adaptive row policy;
- one three-row unknown-numeraire comparison;
- connected and disconnected difference graphs on four coordinates;
- a standard-coordinate quotient; and
- one hand-selected `1/1000` ill-conditioned matrix.

These exact cells are burned.

## Fresh-run separation

The prospective protocol uses different matrix dimensions, coefficients,
rewards, scale factors, adaptive depths, numeraire values, tolerances, and
quotient gauges.

No fresh population law, transcript law, path realization, quotient verdict,
localization error, singular value, or amplification was read before
registration.

## Scientific role

The finite run checks the translation of the theorem into an instrument. The
scale obstruction and `ker(X)=G` criterion are analytic linear-algebra facts.
The run is not evidence that real reward learning has a calibrated numeraire.

