# ASMP-9 exact-tie width theorem v0.4

The v0.4 theorem closes the `delta=0` endpoint separated from v0.3.1:

```text
Q0*(B)=1 for B<=2, and B-1 for B>=3.
```

The proof uses Farey cells to reconstruct bounded primitive coordinate ratios
from exact ternary comparison signs. A prospective verifier checks fresh full
ray universes and sharp lower witnesses.

This remains a cycle-coordinate access theorem, not a complete ASMP-9
resolution.

