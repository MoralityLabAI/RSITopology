# ASMP-9 resolution-obligation matrix after v0.68.1 prereveal amendment

## Verdict

**ASMP-9 remains unresolved.**

The v0.68 measurement quotient remains the correct successor to the rejected
v0.67.1 global-state instrument. Before any new model outcome, v0.68.1 removes
an unjustified inferential claim: a complete sign-inversion orbit over a fixed
scenario registry is an exact sensitivity calculation, not an exact
randomization p-value when arm assignment was not randomized and no symmetric
superpopulation law was registered.

The amended physical bridge now makes only finite-registry claims. Construction
is ready for target-environment registration but has not run because no
authorized A100-80GB offer has appeared below the delegated `$0.70/GPU-hour`
ceiling.

## Status against the five obligations

| Obligation | Strongest evidence after v0.68.1 | Status | Missing resolution evidence |
|---|---|---|---|
| 1. Maximal invariance or identifiability object | Finite reward/cohomology quotients, stochastic and nuisance confusability tiers, and the v0.68 exact common-mode measurement quotient | **Sharp in declared finite grammars; open broadly** | Joint quotient for reward symmetry, physical nuisance, context dependence, misspecification, and non-scalar targets |
| 2. Necessary and sufficient access | Exact finite access theorems through v0.66; v0.67 rejects the global physical transducer; v0.68.1 preserves a local contrast interface without false population inference | **Exact abstractly; physical bridge unvalidated** | Fresh evidence that the local response interface clears its frozen practical margins across construction and confirmation registries |
| 3. Sharp query/sample/intervention bounds | Exact local graph, modulus, menu, and adaptive-tree results | **Exact locally; joint physical complexity open** | Minimax sample/query bounds for a declared stochastic context population; the current fixed registry does not estimate one |
| 4. Robustness to misspecification | v0.67.1 exposes numeric, order, and global-model failure; v0.68 quotients common-mode error; v0.68.1 removes an unlicensed exchangeability assumption | **Failure modes localized; successor unrun** | Fresh singleton calibration, held-out local effects, arm-by-order controls, and a separately registered stochastic generalization model if population claims are desired |
| 5. No-go without coherent latent value | Finite hidden-consideration, state-merging, created-consensus, tier, and global-transducer counterexamples | **Strong finite endpoints; general replacement classification open** | A class-level theorem selecting relation-, kernel-, set-, or path-valued replacements under incoherent demonstrators |

## Correct evidential interpretation

For a fixed registry, all registered model scores are deterministic once model
bytes, tokenizer bytes, prompt bytes, and arithmetic are fixed. Therefore:

```text
finite-registry contrast
  -> measured exactly up to the registered numeric guard;

generalization to a context population
  -> unavailable unless a sampling law or randomized design is separately
     declared.
```

The amended `L0` asks whether at least 10 of 12 scenarios clear all four
target-by-order specificity margins, whether the median worst direction clears
the endpoint epsilon, and whether the overall mean scenario contrast remains
above epsilon. The sign-orbit rank is descriptive only.

## Next load-bearing step

Run construction only after a target Prime environment produces a fresh
prereveal validation and write-once registration binding:

1. the immutable v0.68 protocol;
2. the v0.68.1 amendment;
3. the exact 528-record construction list;
4. amended runner/analyzer/validator bytes;
5. model, tokenizer, environment, and hard resource caps; and
6. no prior outcome access.

Only a passing construction `L0` can open confirmation. Even a two-split pass
would establish an expressed response interface on two finite registries, not
identify moral value or close the reward-shaping classification.
