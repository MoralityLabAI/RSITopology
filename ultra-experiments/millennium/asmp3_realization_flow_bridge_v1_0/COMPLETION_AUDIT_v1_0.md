# ASMP-3 realization-flow bridge completion audit v1.0

## Newly closed

```text
policy -> occupancy flow for frozen acyclic controlled graph = proved/checked
feasible occupancy flow -> policy = proved/checked
terminal law as linear flow image = proved/checked
compact extended law-polytope formulation = explicit
2^k pure-policy band family via 2k flow variables = certified k=1,...,12
full pure-policy extrema enumeration = checked k=1,...,8
adaptive three-state reconstruction = checked
clean-room graph/flow reconstruction = passed
```

This discharges the v0.9 compact-representation assumption for the declared
single-controller acyclic lane.

## Still open

```text
two independently strategic advocates = open
endogenous adaptive verifier strategy = open
perfect-recall two-role sequence-form saddle point = open
hidden-state/imperfect-information controller = open
compact representation of general correlated adaptive noise = open
WV-ADM interface optimization = open
honest-prover computation characterization = open
v0.1 scope adjudication = open/normative
external mathematical review = absent
```

## Non-overclaim rule

The package may be cited as an exact realization-flow bridge for one fully
observed controller.  The controller cannot be reinterpreted as several
independent strategic agents: that would incorrectly convexify products of
their separate strategies.
