# ASMP-3 parent-resolution disposition v2.21

## Mathematical disposition

```text
displayed v0.1 iff = exactly refuted under strict FIX
finite supplied games = exactly certifiable
full standard-encoded FIX membership = undecidable
full standard-encoded semantic-respecting ADM membership = undecidable
encoding choice restores decidability = no, under the five adequacy axioms
FIX/ADM choice restores decidability = no, under the declared information boundary
```

This is the first package in the chain that addresses both blockers left by
v2.19: representation choice and protocol-interface quantification.

## Internal resolution judgment

Under the source's ordinary convention—standard effective encodings of the
canonical game data and no undeclared trusted world channel—the result meets
the explicit negative-resolution rule: the associated full uniform decision
family is undecidable.

The result is not merely a narrow subclass classification. The compiler image
is a many-one reduction into the full decision set. A full-set decider would
decide its image and hence `NONHALT`.

## What remains institutionally open

V0.1 is a candidate definition draft, not a public prize statement. It lacks a
literal machine grammar and complete typed information structure, and it has
zero of the two required independent expert reproductions. Therefore:

```text
internal mathematical negative resolution = proved under every adequate closure
unqualified authorial/normative closure = not machine-provable from the draft
prize-grade/community acceptance = not achieved
```

An authoritative narrower successor may exclude the reduction family, but it
must state that domain restriction. The v2.21 theorem then becomes the sharp
boundary separating the unrestricted program-encoded problem from that new
restricted problem.

## Stop and resume rule

No further finite enumeration can strengthen the universal many-one or
coupling proofs. Resume internal mathematical work only if a proposed successor
domain supplies a restriction that blocks the compiler, in which case analyze
the restricted membership set. Resume parent-acceptance work when an external
team or source authority can review:

1. canonical-instance admissibility;
2. the full-set versus reduction-image logic;
3. the universal inactive-interface coupling; and
4. the adequacy of the intended standard encoding.

Until then, use “internal negative resolution candidate; external acceptance
open,” not “ASMP-3 is impossible” or “prize solved.”
