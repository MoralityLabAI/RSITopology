# ASMP-4 v0.12 positive-dimensional NHIM result

The v0.11 point-fiber repair extends to a genuine compact connected
one-dimensional invariant circle.  On `S^1 x R`, safe feedback gives the global
diffeomorphism

```text
(theta,n) -> (theta+1/4 mod 1,(3/2)n).
```

The invariant circle has tangent norm `1`, unstable inverse norm `2/3`, and
satisfies the checked classical NHIM definition with `lambda=3/4`.  Bounded
interval authority retains the exact local normal-control certificate.

The tangent phase never crosses either port.  Computed and raw sensor
registries therefore retain exact regions `[1,infinity) x [1,infinity)` and
`[2,infinity) x [1,infinity)` with no future-disturbance access.

This removes the zero-dimensional-NHIM objection.  It does not establish
positive-volume robustness or a canonical ASMP-4 solution.

Verification passes nine central gates, eight import-independent checks, nine
focused tests, and the complete 164-test ASMP-4 chain.

The [v0.13 successor](../asmp4_positive_volume_collar_v0_13/RESULT.md) removes
the positive-volume objection with an exact collar theorem; it does not alter
this frozen v0.12 result.
