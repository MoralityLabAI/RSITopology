# ASMP-4 semantic selector audit v0.15

This package red-teams the strongest premise of the v0.14 stop certificate: the inference from absent selector phrases to proved semantic underdetermination.

It performs a complete 23-unit census of the canonical ASMP-4 section. The preregistered conservative rule treats any ambiguous or selecting clause as blocking. The result blocks the strong premise: 15 units are `no_selector`, seven are `ambiguous`, and the split-port liveness clause is `selects_parameterized` because it says to hold the sensor experiment fixed.

The package preserves four evaluation layers separately. A staged five-family held-out pack supports the local selector-risk metric, while the generic publication-style pairwise preflight fails because the canonical source is a fixed 23-unit population rather than a held-out, blinded comparison. The semantic claim is rejected; the operational local-fixture stop is retained only with narrower rationale.

Run:

```powershell
python run_verification.py
python verify_semantic_selector_audit.py
python -m pytest -q test_semantic_selector_audit.py
```

The generated preflight, staging, robustness, row-level clause, and observation artifacts are retained in this directory.

Successor: [v0.16](../asmp4_registered_sensor_classification_v0_16/RESULT.md)
takes the formal-grammar route identified here and classifies all 15 mode
partitions on the collar. The frozen v0.15 disposition remains unchanged.
