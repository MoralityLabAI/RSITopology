# Result

V0.15 finds a material qualification to the v0.14 stopping argument.

The exact positive-volume fork remains intact, but the claim that canonical ASMP-4 is proved semantically underdetermined by selector absence is not supported. A complete conservative census classifies 15 of 23 clauses as no-selector, seven as ambiguous, and one as explicitly treating the sensor experiment as fixed registered instance data. Under the preregistered rule, those eight clauses block the claim.

The local selector-risk metric passes five held-out robustness probe families. Separately, the generic pairwise preflight remains release-blocking because this fixed source census is not held out or blindly judged. These two measurement conclusions are not conflated.

V0.14 therefore requires qualification: stop generating further local fixtures, but do so because the work has reached a formalization and external-adjudication boundary—not because one numeric canonical region has been proved textually underdetermined.

This result does not resolve ASMP-4 and does not invalidate the v0.13 exact collar theorem.

The complete 16-package ASMP-4 regression passes all 194 tests in 226.37
seconds.

The [v0.16 successor](../asmp4_registered_sensor_classification_v0_16/RESULT.md)
formalizes the sensor experiment as an explicit partition parameter and
resolves the local four-mode grammar without reversing this semantic audit.
