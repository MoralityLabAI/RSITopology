# ASMP-12 frame-up correction v0.1

## Origin of the falsified shape

The monotone-threshold expectation did not originate in the registered census.
It came from section F of the supplied frame-up document:

`D:\Download\asmp_candidate_extensions_v0_1.md`

- source SHA-256:
  `2006468eac4758d6db3fc4914db76a81af3e59e1e3b4572a46fbe868b5e1c366`;
- source language: characterize `E(b)` through “monotonicity, threshold b* above
  which the full program-equilibrium folk-theorem set is achieved, and the rate
  of approach”;
- behavioral follow-up: preregister a threshold-shaped cooperation curve in
  verification budget.

The v0.1 finite census falsified that proposed shape for the registered pure
source-table language. `E(b)` can lose equilibria and payoff vectors when a
larger budget admits new deviations. There need not be a single upward
threshold after which previously available cooperative outcomes persist.

## Correction

Replace the monotone-threshold presupposition with a resource-indexed
birth/death question:

1. which equilibrium profiles and payoff vectors appear at each budget;
2. which survive later program admissions;
3. which die through source-label brittleness versus genuinely new behavior;
4. which deaths are robust to payoff and encoding perturbations; and
5. which available equilibria a separately frozen selection dynamic reaches.

This correction does not alter the sealed v0.1 protocol or result. It records
which earlier hypothesis the result rejected and prevents the rejected shape
from silently reappearing in downstream experiments.

