# ASMP-4 v0.11 NHIM/cocycle audit result

**Successor qualification.** V0.12 extends this point fiber to a compact
connected invariant circle with neutral tangent norm `1`, normal inverse norm
`2/3`, and exact classical domination.  Use v0.12 when positive dimension is a
review requirement.

The v0.6 sensor fork survives a stricter positive-class audit, but one part of
its old justification needed repair.  Its binary action set did not by itself
support a differential local-controllability claim.

V0.11 enlarges the bounded plant authority to `[-1,2]`.  Only the same controls
`0` and `1` keep `K={0} x {-3,-1,1,3}` invariant, while a uniform local box is
exactly reachable: for `|n|<=1/6` and `|eta|<=3/4`, the control
`u=q(z)+eta-(3/2)n` lies in the authority interval and reaches `eta`.

The disturbance modes are placed in a bi-infinite full-shift base.  Under safe
feedback, the smooth normal fiber is the invertible cocycle
`phi(k,omega,n)=(3/2)^k n`; `M(omega)={0}` has splitting
`E^u=R`, `E^c=E^s={0}` and satisfies the checked primary random-NHIM definition
for every mode sequence.

Five category-breaking mutations are rejected by the executable audit: a
one-sided base, discrete-only authority, a stale controller, loss of the strict
normal rate gap, and future-mode sensing.

An explicit zero-delay trace audit confirms that the sensor and controller use
only current/past information and never future disturbances.  The two exact
regions remain `[1,infinity) x [1,infinity)` and
`[2,infinity) x [1,infinity)` on the same repaired plant and authority.

This closes a concrete objection to the v0.10 stopping certificate.  It does
not claim ASMP-4 chose this NHIM category or resolve the canonical variational
problem.

Verification passes twelve central gates, eleven import-independent checks,
ten focused tests, and the complete 155-test ASMP-4 chain.
