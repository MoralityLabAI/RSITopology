# ASMP-4 stopping red-team result v0.10

**Successor qualification.** The v0.11 audit identifies a category gap in the
binary-input local-control justification below and repairs it with bounded
interval authority plus an explicit full-shift cocycle NHIM.  The two exact
regions are unchanged.  V0.11 should be used for the strongest positive-class
version of this stopping argument.

## Result

The v0.9 stopping conclusion survives adversarial scope reduction.  Its minimal
proof needs only the v0.6 same-plant rational NHIM sensor fork; the v0.8
stochastic diagonal is not used as a positive-conjecture witness or as a
premise.

Two sensor-domain completions satisfy all 13 explicit architecture obligations
on the same plant while giving different exact regions:

~~~text
computed sensor: [1,infinity) x [1,infinity),
forced raw sensor: [2,infinity) x [1,infinity).
~~~

The central and independent replays also verify all 16 disturbance resets on
the compact safe manifold `K=K_0={0} x {-3,-1,1,3}`: the correct bounded control
returns to `K`, while the other binary control leaves it.

The canonical Markdown does not select either domain, and the machine index is
explicitly non-normative and ungraduated.  A union of registries is not an
implicit repair; it is a third, also-unselected completion.

A bounded primary-source audit covers all four control-under-information-
constraints works cited by ASMP-4.  Each defines one charged information
resource: a single sensor-to-controller alphabet or a single spanning-set
cardinality.  None charges a separate controller-to-actuator transcript or
selects the ASMP-4 raw/computed registry.  Tatikonda and Mitter instead provide
direct evidence that changing the encoder information pattern can change the
required rate.  This is a cited-source scope check, not an exhaustive literature
search or external expert review.

A targeted audit then checks three strong neighboring results that are not in
the canonical citation list.  Kawan-Delvenne's genuine vector rate region uses
coordinates for direct-product plant subsystems; Khojasteh et al.'s two rates
measure payload versus payload-plus-timing on one sensor-controller channel;
and Tomar-Zamani's compositional bound sums scalar IFEs for plant subsystems.
None supplies an ASMP-4 read/write registry selector.  Importing any of the
three requires an additional architecture mapping and sensor-domain choice.
This targeted search is explicitly non-exhaustive and is not external review.

Five selector mutations make this precise.  The unmodified source admits three
distinct targets.  Explicit computed-only, relational-only, raw-only, and
union selectors each make the target determinate; the union selects the
computed rectangle because it contains the other two regions.  None of those
four selectors occurs in the normative source.

## Red-team evidence

Central and import-independent implementations reconstruct the source scope,
machine-index status, rational polynomial embedding, exact mode-word counts
through horizon six, and the two regions.  They seal five resources, resolve
fourteen adversarial counterarguments with zero unresolved cases, and inventory
127 predecessor tests across ten packages.

A separate coordinate audit exhausts all 2,304 coordinate and symbol
relabelings: 24 mode names, two action names, two computed-read names, and 24
raw-read names.  Across 193,536 mode words per registry through horizon three,
every relabeling preserves safety, transcript counts, and both exact regions.

The stochastic lane remains useful as an orthogonal definition gap.  Its
scope is now explicit: a smooth general-setting quantifier counterexample, not
an NHIM classification.

## Disposition

Stop further local enumeration and request a normative sensor/computation
registry.  Reopen for such a registry, a new selecting canonical clause, a
concrete proof error, or a registered class that invalidates the finite
completion analysis.  This is a convincing harness-backed stopping argument,
not a claim that ASMP-4 has one canonical solved region.
