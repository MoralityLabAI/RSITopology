# ASMP-3 simulated ultra-review synthesis v0.6

These reviews are model-generated internal stress tests. They do not qualify as
either of the two independent expert-team reproductions required by the
canonical acceptance policy.

## Combined verdict

```text
probability_core = accept_with_nonmaterial_corrections
complexity_game_semantics = major_revision
release_ready_for_external_acceptance_review = false
current_full_resolution_claim = not_established
```

The exact BSC parity calculation survives review:

```text
TV(P_even,P_odd) = (3/5)^d.
```

The full ASMP-3 claim does not yet survive. The harness proves the reduced
parity experiment and static refutation dimension, but it does not instantiate
or enumerate the entire canonical interactive game.

## Two-layer block

1. **Mathematical/scope block.** Resolve whether “admits a protocol” quantifies
   over message encodings. If it does, the advocates can send all `d=log T`
   bits and a one-query disagreement test has gap `3/5`. If the grammar is
   frozen task data, write a complete game specification and prove that reading
   is the canonical one. Then establish `r_R`, the uniform honest strategy, and
   full-transcript noninterference over every legal case.
2. **External-acceptance block.** After the mathematical repair is resealed,
   obtain two independent attributable teams that each reproduce the end-to-end
   argument. One must provide an independently implemented checker. Simulated
   model reviews cannot fill this gate.

## Convincing stopping boundary

Further enumeration of the current parity harness cannot decide the protocol
quantifier or prove the missing game semantics: every larger grid remains a
calculation inside the same reduced observation model. Work on that harness
architecture should stop until a complete specification or authoritative
scope decision is supplied. The precise external handoff is in
`REVIEWER_HANDOFF_v0_5.md`.
