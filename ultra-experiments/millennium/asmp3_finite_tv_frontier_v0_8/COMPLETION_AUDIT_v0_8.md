# ASMP-3 finite TV frontier completion audit v0.8

## Closed in this package

```text
finite fixed-interface terminal decision theorem = closed
matching primal/dual rational certificates = closed
convex-hull collision counterexample = closed
joint-versus-marginal counterexample = closed
parity BSC family through d=8 = certified
parity all-d formula = proved
benign alias-refinement check r=1,...,8 = certified
clean-room arithmetic reconstruction = passed
```

The theorem gives a necessary and sufficient condition, not only a lower or
upper bound:

```text
optimal uniform gap
  = distance_TV(conv(truthful transcript laws),
                conv(false transcript laws)).
```

## Not closed by this package

```text
v0.1 frozen-versus-existential interface adjudication = open/normative
efficient construction of a separating interaction = open
compact representation of all adaptive strategy/noise laws = open
honest-prover computation lower bounds = open
WV-ADM characterization across admissible interfaces = open
external independent mathematical review = absent
```

The package must therefore be cited as an exact finite typed subtheorem, not as
a resolution of ASMP-3.

## Next mathematical target

For a fixed finite-horizon extensive-form interface, replace explicit law
enumeration by sequence-form realization polytopes.  The candidate target is a
polynomial-size primal/dual formulation when the interaction tree, semantic
noise polytope, and perfect-recall strategy spaces have polynomial-size
descriptions.  This would connect the exact terminal criterion to efficient
interactive protocol analysis without changing the interface quantifier.
