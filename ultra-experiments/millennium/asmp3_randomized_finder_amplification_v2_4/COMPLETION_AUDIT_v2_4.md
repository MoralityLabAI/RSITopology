# ASMP-3 randomized-finder amplification completion audit v2.4

## Newly closed

```text
v0.1 probabilistic-honest-strategy mismatch = identified
Las Vegas-with-FAIL finder contract = frozen
independent post-transcript retry failure (1-alpha)^k = proved
minimal retry count for 72 registry cells = certified
fresh post-witness noise composition = proved
completeness >= 1-delta = proved
soundness <= f+(1-f)delta = proved
gap >= 1-f-2delta+f*delta = proved
all kH retry work = charged
semantic query and message resources = charged
378 finite restart paths = exhaustively audited
inverse-log finder scaling = registered plus held-out confirmation
unique-marker linear retry obstruction = certified
deterministic v2.3 theorem = recovered at alpha=1
clean-room reconstruction = supplied
```

## Still open

```text
normal-form converse extracting a finder from every WV-FIX protocol = open
correlated or history-dependent finder failure = open
interactive/adaptive communication lower bounds = open
WV-ADM interface optimization and attainment = open
universal task-class characterization = open
v0.1 FIX-versus-ADM scope adjudication = open/normative
external mathematical review = absent
```

## Resolution effect

The positive theorem now covers randomized honest finders with explicitly
amplifiable worst-transcript success, rather than only deterministic finders.
The harness also proves that retries cannot evade the v2.1 unstructured-search
cost: constant success still consumes linear total queries.

## Non-overclaim rule

This release composes an assumed randomized finder. It does not derive that
finder from arbitrary protocol soundness and therefore does not close the
universal normal-form or converse obligation.
