# Reviewer packet v0.33

## Review order

1. Read `safe_closing_contract_v0_33.json` for the exact conditional scope.
2. Check the two inclusions in `THEOREM.md`, especially where coordinatewise
   limsup and sublinear vector overhead enter.
3. Check that componentwise support reconstruction is not collapsed across
   irreversible components.
4. Check the Thue-Morse non-eventual-period proof and why a finite exact
   deterministic quotient would imply eventual periodicity.
5. Run both implementations and the focused tests.
6. Confirm the prior-art boundary does not claim novelty for hyperbolic
   shadowing, periodic-orbit entropy formulas, or invariance pressure.

## Load-bearing assumptions

- every infinite safe code has cofinally many safely closable prefixes;
- read and write closing costs are separately sublinear;
- reset-block concatenation has the registered submultiplicative cost law;
- irreversible recurrent components remain indexed; and
- a metric closing construction stays inside the evaluator margin.

## Fast falsification targets

- a safe code with no cofinally closable prefixes;
- an unavoidable linear closing charge on one port;
- hidden controller or actuator memory that is not reset;
- an unsafe connector or shadow error reaching the unregistered boundary; or
- a full-class theorem that derives safe closing from the canonical nonlinear
  hypotheses and thereby makes this conditional layer obsolete.
