# Completion audit v0.24

## Proved

- A parameterized architecture schema makes every known capacity-changing
  registration coordinate explicit without selecting its value.
- Reset concatenation yields multiplicative transcript counts and convex
  normalized log-rate pairs.
- The concave weighted entropy family reconstructs the entire closed
  block-generated region by its supporting half-spaces.
- Coordinate conjugacy and transcript relabeling preserve the family.
- Rectangular, unequal, nonrectangular, raw-refinement, and empty predecessor
  regions are recovered exactly.
- Central and import-independent implementations reject five mutations.

## Verification boundary

The predecessor inventory contains 24 packages and 274 tests. This package
adds 10 focused tests, so the expanded chain contains 284 tests. The explicit
25-package regression passed all 284 tests in 293.92 seconds with Python
bytecode and pytest caching disabled.

## Not claimed

Periodic block completeness is not automatic. Without it, the theorem
characterizes a rigorously defined inner region rather than every
nonstationary infinite safe code. The source-wide nonlinear class grammar and
architecture selection remain external specification obligations.

V0.25 subsequently proves the condition for finite public additive scheduler
graphs within each reachable cyclic SCC and shows why irreversible SCC unions
remain nonconvex and component-indexed. Infinite or hidden sufficient state
continues to lie outside both theorems.
