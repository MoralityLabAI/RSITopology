# ASMP-3 uniform-membership undecidability completion audit v2.20

```text
sealed upstream inputs = 13
producer gates = 10/10
clean-room checks = 10/10
focused tests = 12 passed
cross-package ASMP-3 tests = 470 passed across 41 test files
standalone ASMP-3 checkers = 37/37 successful
external reproductions = 0/2
registered WV-FIX-UCOMP decision problem = resolved as undecidable
unconditional ASMP-3 v0.1 resolution = not established
```

The producer checks exact finite mechanics and source-bound scope. The
clean-room verifier independently reconstructs hashes, pairwise TV values,
resource ledgers, local-atom witnesses, halting transitions, and parent
boundaries. Finite fixtures validate the construction; the universal
undecidability conclusion rests on the written NONHALT reduction.

The standalone total includes the expert-review checker. Its successful result
is `0/2 qualifying; complete=false`, which verifies that the external gate
remains open rather than treating missing review as completion.
