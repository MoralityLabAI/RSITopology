# ASMP-7 excluded-band degradation v0.2

V0.2 extends the registered finite audit across forbidden boundaries
`k1=9..16` and rational truth-report probabilities. It keeps v0.1 immutable.

After registration:

```powershell
python -m pytest ultra-experiments/millennium/asmp7_attestability_frontier/test_boundary_frontier.py -q
python ultra-experiments/millennium/asmp7_attestability_frontier/run_boundary_frontier.py
```

SciPy proposes search locations only. Exact integer binomial tails and rational
adjacency certificates determine every reported minimum.

